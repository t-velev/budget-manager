snapshots:
  - name: dim_account
    relation: ref('stg_account')
    config:
      schema: 03_dwh
      unique_key: account_id_bk
      check_cols:
        - account_id_bk
        - account_name
        - is_archived
